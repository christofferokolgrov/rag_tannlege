def fetch_with_cache(url: str, cache_path, refetch: bool = False) -> str:
    raise NotImplementedError(
        "scraper.fetch.fetch_with_cache is implemented in slice #16; "
        "the empty-manifest skeleton in slice #15 never reaches this path."
    )
